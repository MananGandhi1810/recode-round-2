version: '3.8'

services:
  backend:
    build: ./Backend
    container_name: workspace_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
      - mongodb
    networks:
      - workspace_net

  frontend:
    build: ./Frontend
    container_name: workspace_frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - workspace_net

  postgres:
    image: postgres:15-alpine
    container_name: workspace_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: recode
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - workspace_net

  redis:
    image: redis:7-alpine
    container_name: workspace_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - workspace_net

  mongodb:
    image: mongo:6
    container_name: workspace_mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - workspace_net

  clickhouse:
    image: clickhouse/clickhouse-server:latest
    container_name: workspace_clickhouse
    restart: always
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    networks:
      - workspace_net

  rustfs:
    image: rustfs/rustfs:latest
    container_name: workspace_rustfs
    restart: always
    environment:
      RUSTFS_ACCESS_KEY: rustfsadmin
      RUSTFS_SECRET_KEY: rustfsadmin
      RUSTFS_CONSOLE_ENABLE: "true"
    ports:
      - "9001:9001" # Web Console
      - "9002:9000" # S3 API
    volumes:
      - rustfs_data:/data
    networks:
      - workspace_net

networks:
  workspace_net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  mongodb_data:
  clickhouse_data:
  rustfs_data:
